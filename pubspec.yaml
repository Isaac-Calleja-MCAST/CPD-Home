name: malta_spot_saver
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1        # For the "Brain" (State Management)
  hive: ^2.2.3            # For the "Notebook" (Database)
  hive_flutter: ^1.1.0    # Helper for Hive
  geolocator: ^10.1.0     # For GPS
  image_picker: ^1.0.7    # For Camera
  flutter_local_notifications: ^17.2.2 # For Notifications
  firebase_core: ^4.4.0
  firebase_analytics: ^12.1.1
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
